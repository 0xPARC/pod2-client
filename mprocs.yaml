procs:
  podnet-server:
    cwd: ./
    shell: RUST_LOG=info cargo run --profile release-with-debug -p podnet-server
  podnet-identity:
    cwd: ./
    shell: RUST_LOG=info cargo run --profile release-with-debug -p podnet-ident-strawman
    depends_on:
      - podnet-server
  pod2-client:
    cwd: ./apps/client
    shell: pnpm tauri dev --release
  web-packages:
    cwd: ./
    shell: pnpm i && pnpm build --filter='./packages/**' && TURBO_UI=0 pnpm turbo watch build --filter='./packages/**'
